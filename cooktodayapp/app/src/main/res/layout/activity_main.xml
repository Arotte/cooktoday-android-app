<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

<TextView
    android:id="@+id/tv_main_cooktoday"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:text="Cook Today"
    android:textSize="30dp"

    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintLeft_toLeftOf="parent"
    app:layout_constraintRight_toRightOf="parent"
    app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/bt_main_logout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tv_main_cooktoday"

        android:layout_marginTop="14dp"
        android:text="Logout"
        android:textColor="#D81B60"
        android:onClick="onClick"/>

    <Button
        android:id="@+id/bt_main_recipt"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:layout_below="@+id/bt_main_logout"
        android:layout_marginTop="20dp"
        android:text="recipe"
        android:textColor="#D81B60"
        android:onClick="onClick"/>

    <Button
        android:id="@+id/bt_main_myaccount"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:layout_below="@+id/bt_main_recipt"
        android:layout_marginTop="20dp"
        android:text="My account"
        android:textColor="#D81B60"
        android:onClick="onClick"/>
</RelativeLayout>